<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppShowDoMilhao.MainPage">
    
    <StackLayout Orientation="Horizontal">
        <Button Text="Sortear Fácil" Clicked="Button_Clicked"/>

        <Label Text="{Binding Enunciado}"/>

        <Label Text="{Binding Enunciado}"/>
        <Label Text="{Binding Path=Alternativas[0].Descricao}"/>
        <Label Text="{Binding Path=Alternativas[1].Descricao}"/>
        <Label Text="{Binding Path=Alternativas[2].Descricao}"/>
        <Label Text="{Binding Path=Alternativas[3].Descricao}"/>
 
        <Button Text="Parar" />
        <Button Text="Próxima" Clicked="Button_Clicked_Proxima" />
    </StackLayout>

</ContentPage>
